name: new code AKS

on:
  push:
    branches:
    - main
    - feature/*

jobs:
  terraforminit:
    name: terraform initss
    runs-on: self-hosted
    steps:
    - name: echo command
      run: echo "sam"
