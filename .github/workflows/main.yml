name: new code AKS #optional pipeline name

on:  #workflow kab chalega
  push:
    branches:
    - main
    - feature/*

jobs:
  terraforminit:  #job id
    name: terraform initss #job name
    runs-on: self-hosted #runner and tag dena padta h
    steps:
    - name: echo command
      run: echo "sam"
